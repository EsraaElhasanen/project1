name: automate ci pipeline

on:
  push:
   
    branches: [main]

env: 
  arr: ${{ null }}

jobs:
  
  job1:
   
    runs-on: ubuntu-latest

    steps:

      - name: Create Distro Branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
        with:  
          branch: 'Distro'

      - name: Trigger 1
        run: curl -X POST -u esraa:${{ JENKINSTOKEN }} http://196.219.234.11:8090//job/Backed-CI/job/main/build 
        

  job2:
  
   runs-on: ubuntu-latest
   steps: 
   
      -  name: Checkout
      -  uses: actions/checkout@v2.6.0
         env:
           array1 : git branch -r
           env.arr : array1
           
  job3:
      runs-on: ubuntu-latest
      steps:
      
       run: echo "hhhi"
         
  
 
          

            
